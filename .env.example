# 后端 API 基地址。
# 开发环境默认走 Vite 代理（/api -> http://localhost:8000），无需配置。
# 生产部署时改为后端真实地址，例如 https://api.example.com
VITE_API_BASE_URL=/api

# 仅开发环境生效：Vite 代理转发目标（后端服务地址）
VITE_API_PROXY_TARGET=http://localhost:8000
